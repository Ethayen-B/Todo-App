import React from 'react'

const Sort = () => {
  return (
    <div className='flex gap-2'>
        
            <button 
                onClick={() => {}}
                className={`bg-white  py-1 px-4 rounded text-sm`} >
                A-Z
            </button>

            <button 
                onClick={() => {}}
                className={`bg-white  py-1 px-4 rounded text-sm`} >
                Date
            </button>
        
    </div>
  )
}

export default Sort